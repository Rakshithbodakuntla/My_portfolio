// This portfolio uses plain CSS, not Tailwind CSS.
// Keeping an explicit empty PostCSS config prevents stale Tailwind plugins
// from being loaded by Vite/Vercel.
export default {
  plugins: {},
};
